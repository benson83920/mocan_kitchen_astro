---
import { Image } from 'astro:assets';
import chefImg from '../assets/chef_illustration.png';
---

<section id="about" class="about">
  <div class="container about-container">
    <div class="about-image fade-in-scroll">
      <div class="image-bg-blob"></div>
      <Image src={chefImg} alt="Mocan Chef" class="chef-img" />
    </div>

    <div class="about-content fade-in-scroll">
      <h2>About Mocan</h2>
      <p class="tagline">"Not just a kitchen, but a place to share happiness."</p>
      <p class="description">
        Established in 2017, Mocan Kitchen believes that the best flavors come from the warmth of handmade care. 
        We insist on using natural ingredients and refuse over-processing, bringing you the purest taste of home.
      </p>

      <div class="features">
        <div class="feature-item">
          <span class="icon">🌱</span>
          <span>Natural Ingredients</span>
        </div>
        <div class="feature-item">
          <span class="icon">❤️</span>
          <span>Handmade with Love</span>
        </div>
        <div class="feature-item">
          <span class="icon">☕</span>
          <span>Freshly Made Daily</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    padding: 5rem 0;
    background-color: var(--color-background);
    position: relative;
  }

  .about-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .about-image {
    position: relative; /* For blob positioning */
    display: flex;
    justify-content: center;
  }
  
  .image-bg-blob {
      position: absolute;
      width: 300px;
      height: 300px;
      background-color: #FFF0D4; /* Lighter decor color */
      border-radius: 50%;
      z-index: 0;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
  }

  .chef-img {
    position: relative;
    z-index: 1;
    width: 80%;
    height: auto;
    filter: drop-shadow(0 10px 20px rgba(0,0,0,0.1));
    transition: transform 0.3s ease;
  }
  
  .chef-img:hover {
      transform: scale(1.05) rotate(2deg);
  }

  .about-content h2 {
    font-size: 2.5rem;
    color: var(--color-primary);
    margin-bottom: 1rem;
  }

  .tagline {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--color-decor);
    margin-bottom: 1.5rem;
    font-style: italic;
  }

  .description {
    font-size: 1.05rem;
    color: var(--color-text-main);
    margin-bottom: 2rem;
  }

  .features {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .feature-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.5rem;
    background-color: white;
    padding: 1rem;
    border-radius: 1rem;
    box-shadow: 0 4px 6px rgba(0,0,0,0.05);
    flex: 1;
    min-width: 100px;
  }

  .icon {
    font-size: 2rem;
  }
  
  .feature-item span:last-child {
      font-size: 0.9rem;
      font-weight: 600;
      color: var(--color-primary-dark);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .about-container {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .features {
      justify-content: center;
    }
  }
</style>
